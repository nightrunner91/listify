{
  "name": "listify",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "nuxt": "^3.2.3"
  },
  "dependencies": {
    "sass": "^1.32.8",
    "sass-loader": "^13.2.0"
  }
}
